eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
     healthcheck:
       enabled: true
     serviceUrl:
        defaultZone: http://localhost:8761/eureka
  instance:
    hostname: localhost
        
